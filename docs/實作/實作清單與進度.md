# 階段 3 — 實作：子任務清單與進度

> 本檔為**階段 3（實作）的子任務清單與進度來源**。
> 每次工作前先讀 `CLAUDE.md` 確認主階段，再讀本檔確認細部進度。
> 每完成一個子任務，立即更新本檔的狀態表與 CheckList。

## ⏭️ 下次開工提醒

> 更新此區，讓下次開工能直接知道「現在在哪、下一步是什麼」。

**目前進度**：步驟 1~6 全部完成（2026-06-16）。離線實作階段結案，等待設備到位後執行實機測試（階段 4）。

---

## 技術棧與關鍵決策

| 項目 | 決定 |
|---|---|
| 語言/平台 | C# / .NET 8 |
| 序列通訊 | `System.IO.Ports.SerialPort` |
| Console 光棒選單 | **Spectre.Console**（2026-06-16 拍板） |
| 測試框架 | xUnit（建議）或 NUnit |
| 測試策略 | 先完成實作 + 離線單元測試；設備到位後補實機測試（階段 4） |

---

## 子任務清單

| 步驟 | 內容 | 對應規格書 | 狀態 |
|---|---|---|---|
| 1 | 建立 Solution 結構（.sln / 專案 / 資料夾骨架） | 00 §5 | ✅ 完成 |
| 2 | 傳輸層：`ISerialTransport`、`SerialPortTransport`、`PacketFramer` | 01 §1 | ✅ 完成 |
| 3 | 協定層：`Packet`、`Checksum`、`PacketCodec`、`ResponseParser`、`StatusCode` | 01 §2 | ✅ 完成 |
| 4 | 離線單元測試（階段 -1）：CHECKSUM / 分包 / 編解碼 / 回包解析 | 04 §階段-1 | ✅ 完成（23/23 PASS） |
| 5 | 指令層：`IRm300pClient`、各指令 API、盤點三模式狀態機 | 02 | ✅ 完成 |
| 6 | Console UI：Spectre.Console 光棒選單、選單樹、盤點即時表格、hex 監看 | 03 | ✅ 完成 |

## CheckList

- [x] 步驟 1：Solution 結構建立，`dotnet build` 通過（2026-06-16，0 錯誤）
- [x] 步驟 2：傳輸層完成，`dotnet build` 通過（2026-06-16，0 錯誤）
- [x] 步驟 3：協定層完成，`dotnet build` 通過（2026-06-16，0 錯誤）
- [x] 步驟 4：離線單元測試全數 PASS（2026-06-16，23/23）
- [x] 步驟 5：指令層完成，`dotnet build` 通過（2026-06-16，0 錯誤）
- [x] 步驟 6：Console UI 完成，光棒選單可操作（2026-06-16，0 編譯錯誤）

---

## 專案結構（目標）

```
（方案 RM300P_Tools.sln）
├─ src/
│  ├─ RM300P.Core/
│  │   ├─ Transport/          ISerialTransport, SerialPortTransport, PacketFramer
│  │   ├─ Protocol/           Packet, Checksum, PacketCodec, ResponseParser, StatusCode
│  │   ├─ Commands/           IRm300pClient, Rm300pClient, 各指令 request/response
│  │   └─ Models/             TagReport, DeviceInfo, AntennaSetting …
│  └─ RM300P.ConsoleTool/
│      ├─ Menu/               選單樹、光棒導覽
│      ├─ Views/              盤點即時檢視、設定表單、hex 監看
│      └─ Program.cs
└─ tests/
   └─ RM300P.Core.Tests/      單元測試（CHECKSUM、分包、編解碼可離線跑）
```
