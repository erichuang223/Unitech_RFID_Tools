# 決策記錄 2026-06-16 14:52

## Console 光棒選單實作方式（已拍板）

**決策**：使用 **Spectre.Console** NuGet 套件實作光棒選單導覽。

**理由**：省時省力，內建 SelectionPrompt 與 Live render，適合此類互動式 Console 工具。

**影響範圍**：
- `RM300P.ConsoleTool` 專案需加入 `Spectre.Console` NuGet 依賴。
- 開發規格書 `00_總覽與架構.md` 與 `05_安全非功能與驗收.md` 中「待決事項 #2」已確認，無需再討論。
- 選單互動行為仍依 `03_ConsoleUI與選單規格.md` 定義（↑↓ 移動、Enter 進入、Esc 返回）。

## 測試策略調整（已拍板）

**決策**：設備尚未到位，先完成**實作 + 離線單元測試**，待設備到位後補實機測試（階段 4）。
